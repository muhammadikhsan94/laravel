@extends('layouts.auth')

@section('main-content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-xl-10 col-lg-12 col-md-9">
            <div class="card o-hidden border-0 shadow-lg my-5">
                <div class="card-body p-0">
                    <div class="row">
                        <div class="col-lg-6 d-none d-lg-block bg-login-image"></div>
                        <div class="col-lg-6">
                            <div class="p-5">
                                <div class="text-center">
                                    <h1 class="h4 text-gray-900 mb-4">{{ __('Aplikasi SSO-Laravel UNILA') }}</h1>
                                </div>

                                @if ($errors->any())
                                    <div class="alert alert-danger border-left-danger" role="alert">
                                        <ul class="pl-4 my-2">
                                            @foreach ($errors->all() as $error)
                                                <li>{{ $error }}</li>
                                            @endforeach
                                        </ul>
                                    </div>
                                @endif

                                    <div class="form-group">
                                        <a href="{{ route('signing_process') }}" type="button" class="btn btn-primary btn-user btn-block">
                                            <i class="fas fa-user-alt fa-fw"></i> {{ __('Login with SSO') }}
                                        </a>
                                    </div>

                                    <div class="form-group">
                                        <a href="#" type="button" class="btn btn-github btn-user btn-block">
                                            <i class="far fa-question-circle fa-fw"></i> {{ __('Forgot Password') }}
                                        </a>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
